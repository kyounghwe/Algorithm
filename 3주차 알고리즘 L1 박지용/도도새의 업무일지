#N일간
N = int(input())
B_list = input().split()


#업무일지A 재구성
A_list = []
for i in range(N):
    if i==0:
        A_list.append(int(B_list[0]))
    else:
        A_value = (int(B_list[i]) * (i+1)) - (int(B_list[i-1]) * i)
        A_list.append(A_value)

for a in A_list:
    print(a, end=' ')
